<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="3. praktische Arbeit" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock>Namenssuche</TextBlock>
        <TextBlock Grid.Row="1">Geschlecht</TextBlock>
        <Button Grid.ColumnSpan="2" Grid.Row="2" Click="Button_Click">Suchen</Button>
        <TextBlock Grid.Row="3">Gefundene Namen</TextBlock>
        <TextBlock Grid.Row="4">Alternativen</TextBlock>

        <TextBox Grid.Column="1" x:Name="SearchNameBox"></TextBox>
        <ComboBox Grid.Column="1" Grid.Row="1" x:Name="SexBox">
            <ComboBoxItem Tag="M">maennlich</ComboBoxItem>
            <ComboBoxItem Tag="W">weiblich</ComboBoxItem>
        </ComboBox>
        <ComboBox Grid.Column="1" Grid.Row="3" x:Name="FoundNameBox" SelectionChanged="FoundNameBox_SelectionChanged"></ComboBox>
        <ComboBox Grid.Column="1" Grid.Row="4" x:Name="AlternativeNameBox" SelectionChanged="AlternativeNameBox_SelectionChanged"></ComboBox>

        <ListBox Grid.Row="5" x:Name="GroupByYearBox"  >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Year}" Margin="3" FontSize="11"></TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListBox Grid.Row="5" Grid.Column="1" x:Name="AmountPerYearBox">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Count}" Margin="3" FontSize="11"></TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
