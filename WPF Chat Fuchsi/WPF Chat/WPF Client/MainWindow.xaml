<Window x:Class="WPF_Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid x:Name="loginFenster">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <Label Content="Benutzername" HorizontalAlignment="Center"></Label>
            <TextBox x:Name="username"></TextBox>
            <Label Content="Passwort" HorizontalAlignment="Center"></Label>
            <TextBox x:Name="password"></TextBox>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Login" Width="100" Margin="10" Click="Button_Click_Login"></Button>
                    <Button Content="Registrieren" Width="100" Margin="10" Click="Button_Click_Registrate"></Button>
                </StackPanel>
            </StackPanel>
        </Grid>

        <StackPanel x:Name="chatFenster" VerticalAlignment="Center" Visibility="Collapsed">
            <TabControl x:Name="MeinTabControl" SelectionChanged="ChangedTab">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" />
                    </DataTemplate>
                </TabControl.ItemTemplate>

                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Height="300">
                            <ItemsControl ItemsSource="{Binding DataContext.messages, RelativeSource={RelativeSource AncestorType=Window}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#EEE" Margin="5" Padding="8" CornerRadius="5">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Username}" FontSize="14" Foreground="Gray" />
                                                <TextBlock Text="{Binding Title}" FontSize="12" Foreground="Gray" />
                                                <TextBlock Text="{Binding TheMessage}" FontSize="10" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>

            <Border BorderBrush="#CCC" BorderThickness="0,1,0,0" Margin="0,10,0,0" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Titel:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                    <TextBox x:Name="txtTitle" Grid.Column="1" Margin="0,0,0,5" Padding="3"/>

                    <TextBlock Text="Nachricht:" Grid.Row="1" VerticalAlignment="Top" Margin="0,5,10,5"/>
                    <TextBox x:Name="txtMessage" Grid.Row="1" Grid.Column="1" 
                 Height="60" AcceptsReturn="True" TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto" Margin="0,0,0,5" Padding="3"/>

                    <Button Content="Nachricht senden" Grid.Row="2" Grid.Column="1" 
                HorizontalAlignment="Right" Width="120" Padding="5"
                Click="Button_Click_SendMessage" Background="#007ACC" Foreground="White" FontWeight="Bold"/>
                </Grid>
            </Border>

        </StackPanel>
    </Grid>
</Window>
